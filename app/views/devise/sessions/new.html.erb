<div class="col-md-10 mx-auto mt-5 pt-3">

  <div class="text-danger">
    <%= flash[:alert] %>
    <%= flash[:error] %>
  </div>

  <h2 class="text-center">ユーザーログイン 🦈</h2>

  <div class="text-center mt-5 mb-3"><h7>会員の方はこちらからログイン</h7></div>

  <%= form_with model: @user, url: user_session_path, local: true do |f| %>
    <div class="text-center field">
      <%= f.label :メールアドレス %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email" %>
    </div>

    <div class="text-center field">
      <%= f.label :パスワード %><br />
      <%= f.password_field :password, autocomplete: "current-password" %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="text-center mt-3 field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me %>
      </div>
    <% end %>

    <div class="text-center actions">
      <%= f.submit "ログイン", class:"btn btn-dark", style:"width:200px" %>
    </div>
  <% end %>

  <div class="text-center pt-5">
    <%= link_to "Googleアカウントでログイン", user_google_oauth2_omniauth_authorize_path, method: :post %>
  </div>

  <div class="text-center pt-3">
    <h7>会員登録がお済みでない方　↓</h7>
    <p><%= link_to "こちら",new_user_registration_path %></u>　から新規登録してください。</p>
  </div>

</div>
