<h1 class="text-center mt-5 mr-5">ブックマーク一覧</h1>

<div class="container col-md-6 mb-5">
  <div class="card-list-container pt-3 row">
    <% @bookmarks.each do |bookmark| %>
      <div class="card mr-2 mt-4 shadow-lg">
        <%= link_to dive_path(bookmark.dive) do %>
          <%= attachment_image_tag bookmark.dive, :image, :fill, 300, 230 ,class:"card-img-top py-2 px-2";%>
        <% end %>
        <div class="card-body">
        <div class="text-center card-title">
          <div class="mb-2">
            <%= bookmark.dive.created_at.strftime('%Y/%m/%d') %>
          </div>
          <%= attachment_image_tag bookmark.dive.user, :profile_image, size: "25x25", class: "rounded-circle" %>
          <%= link_to bookmark.dive.user.name, user_path(bookmark.dive.user_id) %>
        </div>
        <div class="text-center card-title"><%= bookmark.dive.title %></div>
        <div class="text-center card-title">❤️<% bookmark.dive.favorites.count %>　・　💬<%= bookmark.dive.dive_comments.count %></div>
        </div>
      </div>
    <% end %>
  </div>
</div>